# Tests skipped by run-tests.ps1 unless -All or -Test names them.
# One name per line; text after # is the reason.

TestCmdRepeatedDirNoLineLoss  # fails: "prompt ready after priming" times out
TestLineBufferedBackspace  # slow live shell session (~16s)
TestPsBackspaceFlow  # fails: prompt not captured, backspace check sees "abc   "
TestPsCmds  # slow live shell session (~50s)
TestPsCommands  # slow live shell session (~22s)
TestPsDirBetween  # slow live shell session (~20s)
TestPsDirOnly2  # slow live shell session (~17s)
TestPsDirTiming  # slow live shell session (~22s)
TestPsEchoThenLs  # slow live shell session (~20s)
TestPsPersistent  # slow live shell session (~19s)
TestPsSequence  # slow live shell session (~22s)
TestPsWhich  # slow live shell session (~38s)
TestColumnModeMenuController  # fails: 4 checks (menu hit-testing)
TestDrivePopup  # slow (~20s): waits on drive detail, long on unreachable network drives
TestFileFind  # does not compile: TFindDoneCallback signature changed
TestZipNav  # needs a prepared fixture Z:\Temp\mtn2-zip-parent-bug\test.zip
