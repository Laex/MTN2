# Program screen

From top to bottom:

- **Top menu** — the Left / Files / Edit / Commands / Options / Right categories and a clock. Opened with **F9**, **Alt** or a click.
- **Workspace tabs** — sets of panels and terminals (see [Tabs and workspaces](workspaces.md)).
- **Two file panels** — the active one is highlighted by its frame; **Tab** switches sides. Drive letters are shown on the bottom edge of a panel.
- **Command line** — a prompt with the current folder.
- **F-key bar** — hints for the actions of the current mode; you can click them. While Shift / Ctrl / Alt is held, the bar shows the matching actions.
- **Status line**.

The **console** lives under the panels: **Ctrl+O** (or **Esc** with an empty command line) hides the panels and shows the command output.

**F1** — help on what is on screen (panels, viewer, the open dialog or menu); **F1** inside the help — the contents. **F10** — quit.

---

[Contents](index.md)
